<!DOCTYPE ui:UiBinder SYSTEM "http://dl.google.com/gwt/DTD/xhtml.ent">
<ui:UiBinder xmlns:ui='urn:ui:com.google.gwt.uibinder'
             xmlns:g='urn:import:com.google.gwt.user.client.ui'>
    <ui:style>
        .error {
            color: #ff4500;
        }

        .box {
            padding: 15px;
        }

        .buttonPanel {
            width: 100%;
            background-color: #d0e4f6;
        }

        .buttonTable {
            margin-left: auto;
            margin-right: 0px;
            padding: 2px;
        }

        .button {
            width: 75px;
        }
    </ui:style>
    <g:DialogBox ui:field="dialogBox" text="Failure Notification" animationEnabled="true" modal="true"
                 glassEnabled="true">
        <g:VerticalPanel>
            <g:HTMLPanel styleName="{style.box}">
                <h3 class="{style.error}">Ooops, something went wrong!</h3>
                <g:Label ui:field="messageLabel">...</g:Label>
            </g:HTMLPanel>
            <g:DisclosurePanel ui:field="stacktracePanel" title="Show Stacktrace" animationEnabled="true" visible="true">
                <g:Label ui:field="stacktraceLabel" height="50" wordWrap="true">...</g:Label>
            </g:DisclosurePanel>
            <g:HTMLPanel styleName="{style.buttonPanel}">
                <table class="{style.buttonTable}">
                    <tr>
                        <td>
                            <g:Button ui:field="closeButton" addStyleNames="{style.button}">Close</g:Button>
                        </td>
                    </tr>
                </table>
            </g:HTMLPanel>
        </g:VerticalPanel>
    </g:DialogBox>
</ui:UiBinder>